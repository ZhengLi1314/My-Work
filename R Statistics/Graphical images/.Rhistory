tvhours <- read.csv('tv_hours-2'); tvhours
tvhours <- read.csv('tv_hours-2.csv'); tvhours
hist(tvhours$hours)
hist(tvhours$hours, col = 'green', breaks = 10)
hist(tvhours$hours, col = 'green', breaks = 30)
